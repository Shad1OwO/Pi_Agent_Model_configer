put the file in

C:\Users\Your_Username\.pi\agent\extensions
if your doesn't have extensions folder you can manually create it

open pi
use /setup-model-json
enter all the information it ask for

the extensions will fetch all the context size and model for you via /v1/models
